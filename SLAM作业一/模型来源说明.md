# 模型来源与核验范围

核验日期：2026-09-23。

## 可确认的来源链

现有 `data/model_metadata.json` 记录：输入源名为 `RMUC2025.stp`，经过网格导出和坐标变换生成 `data/RMUC2025.STL`。坐标变换为 `(x,y,z)=(X,-Z,Y)`，模型单位保持毫米，导出记录中地面为 z=0。ROS 转换器再使用 `scale=0.001` 转为米。

源 STEP 的记录 SHA256：

```text
a3c385e6918f3dbc290dc14d1f90be19adce4a988ff3f8fb2d4a3ddec07fc48a
```

当前实际 STL 的重新计算 SHA256：

```text
d6c9da11f09604db0c2bbe8e368b7f6176de36b1039edac401aee69264f14f31
```

实测文件大小为 389,472,984 字节；二进制 STL 头记录 7,789,458 个三角面；大小满足 `84 + 50 × 三角面数`。三项均与既有转换记录相符，说明当前文件与先前记录的转换产物一致。预览图片直接由此 STL 渲染

